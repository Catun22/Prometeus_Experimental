# Prometheus_Experimental - v1.0.0 "Divide et Impera"

Вход в программу: `__main__.py`. 

Минимально проект требует установки библиотеки `rich`.

Установка `rich`:

```bash
pip install rich
```

Для полноценной работы проекта, помимо `rich`, требуется библиотека `colorama`.

Установка `colorama`:

```bash
pip install colorama
```

Или установка `colorama` и `rich` одновременно:

```bash
pip install -r requirements.txt
```

Пользователям Windows рекомендуется использовать Windows Terminal в качестве используемого терминала для этой программы.

## О **Prometheus_Experimental**

***Prometheus_Experimental*** - это универсальный и расширяемый инструмент управления знаниями.

### Цель

Цель **Prometheus_Experimental** - оказывать помощь пользователю по управлению и хранению его знаний. 

### Задачи

Задачи **Prometheus_Experimental**:

1. Создание готовых шаблонов под конспекты пользователя в файлах с расширение markdown (md).

Программа, по поручению пользователя, создаст markdown файл с готовым шаблоном внутри. Директория создания файла указывается пользователем.

2. Просмотр созданных шаблонов.

Программа позволяет легко находить и выводить в терминал названия созданных файлов markdown.

3. Редактирование созданных шаблонов в редакторах.

Программа позволяет редактировать шаблоны в специальных редакторах или средах разработки. На ОС Linux предусмотрен выбор редактора, например: vim, nano, code и т.д..

4. Адаптация под пользователя.

Программа достаточно гибкая и имеет возможность выполнить любые фантазии пользователя, зависящие лишь от его собственного мастерства.

#### Дополнительные задачи

Дополнительной задачей проекта является открытие страниц в браузере.

Предполагается, что пользователь будет черпать основной пласт новой информации и знаний из сети Интернет - следовательно открытие страниц в браузере одна из наиболее часто используемых функций этой программы.

Пользователь может вводить ссылки на сайты вручную или создать собственную функцию, в которую он может поместить готовую ссылку на сайт и использовать её в меню (но об этом дальше 🤫).

> #### ***Prometheus_Experimental*** - "***менеджер"*** ваших знаний.

### Структура проекта

Структура проекта Prometheus_Experimental выглядит следующим образом:

```bash
.
├── 📁 core
│   ├── 📁 configs
│   │   ├── ⚙️ actions.json
│   │   ├── ⚙️ colors.json
│   │   ├── ⚙️ config.json
│   │   └── ⚙️ menu.json
│   ├── 📁 lib
│   │   ├── 📁 helper
│   │   │   └── 🐍 helpers.py
│   │   ├── 📁 prompts
│   │   │   ├── 🐍 prompts_menu.py
│   │   │   └── 🐍 prompts_system.py
│   │   ├── 📁 support_actions
│   │   │   ├── 🐍 browser_actions.py
│   │   │   └── 🐍 settings_actions.py
│   │   ├── 📁 system_apps
│   │   │   └── 🐍 findler.py
│   │   ├── 📁 templates
│   │   │   ├── 🐍 patterns.py
│   │   │   └── 🐍 templates.py
│   │   ├── 🐍 actions.py
│   │   └── 🐍 path_manager.py
│   ├── 📁 utilits
│   │   ├── 🐍 cleaner.py
│   │   ├── 🐍 error_catcher.py
│   │   ├── 🐍 installer.py
│   │   └── 🐍 opener_json.py
│   ├── 🐍 loader.py
│   └── 🐍 menu.py
├── 📁 user
│   ├── 📁 configs
│   │   ├── ⚙️ actions.json
│   │   ├── ⚙️ colors.json
│   │   ├── ⚙️ config.json
│   │   └── ⚙️ menu.json
│   ├── 🐍 actions.py
│   ├── 🐍 patterns.py
│   └── 🐍 prompts.py
├── 🐍 __main__.py
├── 📄 README.md
└── 📄 requirements.txt
```

### Концепция работы **Prometheus_Experimental**

Как видно из схемы, проект Prometheus_Experimental разделен на две директрии или два основных "блока" - core и user.

#### core

core - основополагающая директория, "движок" всего проекта. Внутри этой директории содержится весь "арсенал" работы программы.

Наиболее важными модулями этой директории являются: 

1. menu.py - отвечает за всё меню программы.
2. loader.py - загружает пользовательские модули.
3. path_manager.py - отвечает за все пути в проекте.
4. actions.py - хранит методы, реализующие цель и задачи проекта.
5. patterns.py - хранит самый главный класс TemplateMd, создающий все markdown файлы проекта.
6. helpers.py - содержит самые "низкие", "базовые" методы, дополняющие actions.py
7. prompts_menu.py (prompts_system.py) - содержит раскрашенные промпты.

Пользователю, для успешного взаимодействия с программой не нужно знать все тонкости программы.
Достаточно понимать суть следующих модулей в core:

1. actions.py
2. patterns.py
3. prompts.menu.py

А также понимать суть следующих конфигурационных файлов:

1. menu.json
2. config.json
3. actions.json
4. colors.json

#### user

Директория user не является обязательной для работы всей программы. Однако.
Благодаря директории user, пользователь может добавлять в программу свои собственные шаблоны, свои собственные методы или функции, свои собственные пути, свои собственные цвета и промпты.

> Именно директория user является "душой" проекта, поверх неприступной директории core.

Из схемы можно увидеть соответствие между названими модулей в core и user.

1. core/lib/actions.py соответствует user/actions.py
2. core/lib/templates/patterns.py соответствует user/patterns.py
3. core/lib/prompts/prompts_menu.py соответствует user/prompts.py
4. core/configs/\*.json соответстветствуют user/configs/\*.json

Таким образом, образно говоря, в проекте организована "примитивная" система "плагинов".

Пользователь может писать свои собственные классы, методы и функции внутри вышеуказанных модулей.
Более того, пользователь может создать дополнительные модули внутри директории user.

### Правила пользования проектом Prometheus_Experimental

Prometheus_Experimental просит соблюдать несколько общих правил:

1. Не касайтесь модулей, не изменяйте модули внутри директории core без необходимости для этого.

Директория core является "двигателем" проекта - это отточенная структура и без необходимости, например, при возникновении непредвиденных ошибок, не стоит изменять что-либо внутри core.

2. Наследуйтесь от классов внутри модулей: actions.py (class CLI), patterns.py (class TemplateMd), prompts_menu.py (class UserPrompts).

Эти классы предназначены для того, чтобы пользователь, при создании собственных шаблонов или функций наследовался от этих классов.

3. Пишите свои конфигурационные файлы из user/configs menu.json, actions.json, config.json и colors.json - в противном случае их конфигурация будет взята в core/configs

По умолчанию конфигурационные файлы в user/configs уже существуют, идентичные конфигурационным файлам из core/configs.

Также Prometheus_Experimental просит соблюдать следующие частные правила:

1. В модуле user/actions.py обязательно должен присутствовать класс MyCLI, наследуемый от класса CLI в core/actions.py.

2. Новые методы, добавляемые в MyCLI могут быть схожими с методами из CLI и не схожими - то есть, то есть, либо из CLI, либо из другого модуля (который пользователь сможет создать самостоятельно, если решит увеличить функционал), но, чтобы эти методы могли работать, необходимо:
    1. Чтобы имя метода совпадала со значением словаря в actions.json - иначе - вы не сможете взаимодействовать с вашим методом.
    2. Чтобы найти этот метод, вы должны создать меню в menu.json в виде словаря, и, значение этого словаря должно совпадать с ключом внутри actions.json.

    Получается следующее: В меню определяется промежуточное имя для вызова метода, оно передается в actions.json, при совпадении значения из menu.json и ключа в actions.json, значение из actions.json ищется в модуле actions.py - при нахождении исполняется.

    В иных случаях вы наткнетесь на ошибку, либо на специальную заглушку.

    3. Если вы создали новый метод, идентичный методам из core/actions.py - скорее всего, это метод по созданию какого-либо шаблона, ты вам будет необходимо определить для него путь. Для этого в методах create_item и look_item существует аргумент path_key, который должен быть идентичен значению ключа в config.json, для создания шаблона по пути. Если вы не укажете этот ключ (и путь) или укажете несуществующий - возникнет ошибка KeyError.

Самое важное правило Prometheus_Experimental:

1. Экспериментируйте. Добавляйте собственные модули, расширяйте функционал, учитесь, храните знания.

Вы можете легко добавить новый функционал в эту программу, расширить его под себя.

#### Obsidian

Одним из важных аспектов программы - является возможность взаимодействия с программой Obsidian.

Вы можете сделать следующее: 

Создать одну директорию, где будут храниться все ваши конспекты и привязать эту директорию к Obsidian. Так вы сможете легко создавать конспекты из терминала.

Далее, создайте отдельный модуль в user, который мог бы вызывать Obsidian на выполнение и подвяжите его "выход" к "входу" (методу) в user/actions.py (и не забудьте про конфигурационные файлы).

Если вы переживаете за свои конспекты, то вы также можете создать функцию для "бэкапа" директории с вашими конспектами в отдельную директорию, или вовсе отправлять эти конспекты в облако.


> ### *"Cogito ergo sum"*

### Пример

```python

# Начальное меню:
╭─ Cogito ────────────────────╮
│ Добро пожаловать, господин!   │
╰────────────────── ergo sum ─╯
──────────── МЕНЮ ────────────
1. Открыть
2. Работать
3. Система
4. Настройки

[b] - назад | [q] - выход
Чем вы займетесь сегодня?
>>> 
```
